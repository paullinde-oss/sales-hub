# BMP Supplies — Sales Hub

Internal sales tool for quoting, product management, DIMS, and shipping.

## Deploy

```bash
npm install
npm run dev       # local preview at localhost:5173
npm run build     # production build → dist/
```

Push to `main` branch → GitHub Actions builds and deploys automatically.

Live at: https://saleshub.bmpsupplies.com
